# 数字空间（My Melody Space）领域词汇表

## 产品
- **数字空间（美乐蒂空间）** — 产品名。以 My Melody 治愈风呈现的个人数字空间（任务 / 目标 / 知识库 / Melo 助手）。
- **归档单文件** — 旧版纯前端单文件应用 `my-melody-space.html`，完全离线可用；仅存档，不再演进。
- **新版（全栈版）** — 当前演进方向：需要登录，数据存放在服务器。

## 用户与账号
- **用户** — 使用数字空间的人，拥有唯一用户名、昵称和密码。
- **用户名** — 登录标识，全局唯一。
- **昵称** — 展示名，用于首页问候语。
- **会话** — 登录后保持的登录态，默认 7 天有效。
- **注册 / 登录 / 退出** — 账号生命周期的基本操作。

## 数据实体
- **任务** — 待处理的最小事项单元。状态机：待完成 → 已完成。属性：优先级（高/中/低）、分类（工作/学习/生活/其他）、截止日期（可选）；可挂载到目标下作为子任务（goalId）。
- **目标** — 中长期想达成的事。属性：截止时间（可选）、子任务列表（关联任务）；进度由子任务完成情况自动统计（已完成子任务 / 子任务总数），不接受手动设置。
- **笔记（知识记录）** — 收藏的想法。属性：分类（学习/灵感/收藏/其他）、标签（逗号分隔字符串）。
- **完成率** — 已完成任务数占任务总数的比例（今日口径有单独统计）。
- **今日状态** — 首页卡片，展示今日任务/完成情况；无今日任务时展示全部任务的完成情况。
- **当前目标** — 进度最低的目标，首页重点展示。

## Melo
- **Melo** — 数字空间内置助手角色名。本地规则引擎 + 可选真实大模型（LLM）：AI 拆解 / 今日规划优先调用 LLM（未配置 Key 或调用失败时自动回退本地规则），智能排序 / 建议 / 笔记整理 / 成长总结基于本地规则。
- **Melo整理** — 对笔记生成摘要、关键词、分类建议。
- **Melo智能排序 / 今日规划 / AI拆解 / 成长总结** — Melo 提供的四类功能（任务分析已移除）。

## 数据与同步
- **服务器为准** — 登录后所有数据以服务器为唯一事实来源（换设备登录即可见同一份数据）。
- **导入** — 把归档单文件导出的 JSON 一次性迁入当前账号（注册/登录后的迁移动作）。
- **备份 / 恢复** — 设置页中的 JSON 导出与导入（账号内数据的本地备份手段）。
- **离线** — 新版仅缓存静态壳（App Shell），断网时给出提示；不承诺数据可用。真正离线可用是归档单文件的特性。

## 工程
- **同源** — 前端与 API 部署在同一域名下（Express 托管 `public/`），无 CORS。
- **public** — 前端静态资源目录；**server** — 后端服务目录。
- **PWA** — 可添加到主屏幕的网页应用（App Shell 缓存 + 清单文件）。
- **零外部资源** — 新版不使用 CDN / 外部字体，字体走系统字体栈。